# TasraPostaManager — Ortam Değişkenleri
# Bu dosyayı .env olarak kopyalayın ve gerçek değerleri girin.

# Gmail SMTP şifresi (Google App Password)
TasraPosta__EmailSettings__SenderPassword=BURAYA_GMAIL_APP_PASSWORD

# SQL Server Connection String (opsiyonel, appsettings.json'daki değer önceliklidir)
# ConnectionStrings__SqlConnection=Server=localhost;Database=TasraPostaDB;Trusted_Connection=true;TrustServerCertificate=true;
